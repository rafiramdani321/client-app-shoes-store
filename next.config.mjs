/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com", "c78h8pe176.ufs.sh", "lh3.googleusercontent.com", "github.com"
    ]
  }
};

export default nextConfig;
